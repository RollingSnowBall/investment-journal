# 대화 → 원장 글

두 단계로 나눈다.

1. **추출** — 투자 이야기를 나눈 그 대화 창에 [1단계 프롬프트](#1단계--추출-프롬프트)를 붙여넣는다.
   대화에서 재료만 뽑아 구조화된 메모로 만든다. 아직 글이 아니다.
2. **작성** — 그 메모를 새 창(또는 Claude Code)에 가져와 [2단계 프롬프트](#2단계--작성-프롬프트)와 함께 준다.
   원장 양식에 맞춘 본문과 `index.json` 한 줄이 나온다.

나누는 이유는 두 가지다. 대화 세션은 맥락을 다 갖고 있지만 길고 어수선해서,
양식까지 같이 신경 쓰게 하면 둘 다 어설퍼진다. 그리고 재료와 글을 분리해 두면
**대화에 없던 걸 지어냈는지**를 중간에서 눈으로 잡을 수 있다.

한 번에 끝내고 싶으면 대화 창에 두 프롬프트를 이어 붙여도 된다. 다만 결과는 대체로 덜 좋다.

---

## 1단계 — 추출 프롬프트

투자 이야기를 나눈 대화 창 맨 끝에 아래를 붙여넣는다.

### (여기부터 복사)

지금까지 나눈 대화에서 **투자 기록으로 남길 재료만** 뽑아줘.
아직 글을 쓰는 게 아니다. 나중에 글로 옮길 메모를 만드는 단계다.
아래 양식을 그대로 채워서 출력해줘. 다른 설명은 붙이지 마.

```
## 원장 재료

**시점**: YYYY-MM (이 판단을 한 때. 대화 날짜가 아니라 판단 시점)
**티커**: 대화에 나온 것만, 중요한 순서대로
**실행 여부**: 실행함 / 아직 안 함 / 안 하기로 정함

### 결정 또는 현재 상태
한두 문장. 무엇을 했는지, 또는 무엇을 정했는지.

### 그렇게 판단한 근거
- 논리적으로 설명 가능한 이유만 여기에.

### 감정·직관이 개입한 지점
대화 중 "느낌", "무섭다", "아쉽다", "적응이 안 된다", "별로다" 같은
말이 나온 대목을 그대로 적는다. 판단을 흐렸을 수 있는 대목이면 표시해 둘 것.
없었으면 "없음"이라고만 쓴다. 절대 지어내지 마라.

### 숫자나 자료로 확인한 것
- 대화에서 실제로 확인했다고 말한 것만.
- 확인 안 하고 넘어간 건 여기 쓰지 마라.

### 확인하지 못한 것 / 아직 모르는 것
- 

### 남은 조건
무엇이 충족되면 무엇을 할 것인가. 대화에서 정해지지 않았으면 "미정".

### 원칙 후보
이번 판단에서 한 줄짜리 원칙으로 떨어지는 게 있으면 적는다. 없으면 "없음".

### 빠진 것
위 항목 중 대화에 근거가 없어 채우지 못한 것. 글 쓸 때 지어내면 안 되는 부분.
```

지킬 것:

- **대화에 없는 내용을 만들지 마라.** 비어 있으면 비어 있다고 적는 게 맞다.
  특히 "확인한 것"에 확인하지 않은 걸 넣지 마라.
- **금액·수량·총자산·계좌 정보는 전부 빼라.** 공개 저장소에 올라간다.
  "절반 넘게", "3%대", "20~80달러 구간" 같은 상대적 표현으로만 남긴다.
- **내 말과 네 말을 구분하지 마라.** 대화에서 합의된 결론만 남기면 된다.
  누가 무슨 말을 했는지는 기록에 필요 없다.
- 대화가 여러 종목을 오갔으면 **한 편으로 묶일 만한 것만** 추린다.
  성격이 다른 판단이 섞여 있으면 그렇다고 알려주고, 어떻게 쪼갤지 제안해줘.

### (여기까지 복사)

---

## 2단계 — 작성 프롬프트

1단계 결과를 붙여넣고 아래를 함께 준다.
Claude Code에 주면 파일 생성부터 push까지 한 번에 된다.

### (여기부터 복사)

위 재료로 내 투자 원장에 올릴 글 한 편을 써줘.

### 글의 목적

정답을 적는 게 아니라 **그때의 근거를 남기는 것**이다.
6개월 뒤의 내가 읽고 "그때 왜 그렇게 판단했는지"를 재구성할 수 있어야 한다.
결과가 틀렸더라도 근거가 남아 있으면 그 글은 성공이다.

### 구조

H1은 쓰지 마라(제목은 메타데이터에서 온다). 본문은 이 순서로 간다.

1. **첫 문단 — 결론 먼저.** 한두 문장으로 무엇을 했는지 또는 무엇을 정했는지 바로 적는다.
   핵심 행동에만 볼드를 건다. 그다음 문장에서 남은 문제를 제기한다.
   예: "8월에 전략적인 선택을 하나 했다. **SCHD를 절반 넘게 팔고, 그 돈으로 UNH를 추가로 매집**했다."
   예: "결론부터 적으면, **아직 아무것도 들고 있지 않다.**"

2. **H2 3~4개.** 대체로 이 흐름을 따른다 — 근거 → 자기 검증 → 다음 행동.
   H2 제목은 라벨이 아니라 그 절이 답하는 질문처럼 쓴다.
   좋음: "왜 UNH였나" / "내 조심스러움의 정체" / "진짜 이유부터 분리하기" / "들어간다면"
   나쁨: "분석" / "배경" / "결론" / "시장 전망"
   H3는 쓰지 않는다.

3. **자기 검증 절은 반드시 넣는다.** 이 원장의 정체다.
   재료의 `감정·직관이 개입한 지점`을 여기서 쓴다.
   내 판단에서 **논리와 감정을 분리**하는 절이 한 개는 있어야 한다.
   "이건 근거가 있는 판단인가, 아니면 이미 내린 결론을 정당화하는 중인가?"
   재료에 그 항목이 "없음"이면 억지로 만들지 말고, 대신
   `확인하지 못한 것`을 정직하게 적는 절로 대체한다.

4. **마지막 — 열린 상태로 끝낸다.** 깔끔하게 매듭짓지 마라.
   재료의 `남은 조건`을 조건부 다음 행동으로 쓴다.
   예: "지금은 관망. 진입은 리스크 설계가 끝난 뒤에."
   예: "다시 기회가 온다면 SCHD를 더 팔아서라도 UNH를 더 살 의향이 있다."

5. **인용문은 선택.** 재료의 `원칙 후보`가 한 줄로 깔끔하게 떨어질 때만 맨 끝에 `>` 로 붙인다.
   억지로 만들지 마라. 기존 3편 중 2편에만 있다.

### 문체

- 1인칭, 종결어미 `-다`. 존댓말·설명체 금지.
- 담백하게. "~인 것 같다", "~라고 생각한다" 같은 완충어를 빼고 단정한다.
- 볼드는 **판단·기준·결정**에만. 문장 전체를 굵게 하지 않는다. 한 절에 한두 번이면 충분하다.
- 분량 150~350단어. 짧을수록 좋다. 30%를 지워서 더 좋아지면 계속 지운다.
- 남에게 설명하는 톤이 아니라 나에게 적는 톤. 독자를 가르치지 않는다.

### 표·불릿

- **표**는 비교 대상이 3개 이상일 때만. (예: BTC / MSTR / COIN의 리스크 구조)
- **불릿**은 기준이나 선택지를 나열할 때만.
- 둘 다 없어도 되는 글이면 넣지 않는다. 산문이 기본이다.

### 반드시 지킬 것

- **재료에 없는 사실·숫자를 지어내지 마라.** 재료의 `빠진 것` 항목은 글에서도 비워 둔다.
  근거가 약한 대목은 약하다고 적는 게 맞다.
- **대화를 그대로 옮기지 마라.** "내가 물었더니 Claude가 답하길" 같은 형식 금지.
  결론만 내 목소리로 다시 쓴다. Claude를 언급할 필요 없다.
- **금액·수량·총자산·계좌 정보 금지.** 공개 저장소다.
- **태그는 아래 6개 중 정확히 하나.** 새로 만들지 마라.
  - `리밸런싱` — 실제로 사거나 팔았을 때
  - `관점정리` — 아직 실행하진 않았고 생각을 정리했을 때
  - `관망` — 의도적으로 아무것도 하지 않기로 정했을 때
  - `리서치` — 특정 종목이나 섹터를 조사한 내용
  - `회고` — 과거의 판단을 돌아볼 때
  - `되새김` — 종목과 무관하게 얻은 것. 티커 없이(`"tickers": []`) 올려도 된다.

`되새김` 글은 구조가 조금 다르다. 판단이 아니라 생각을 남기는 글이라 첫 문단에
결정 대신 **이번에 무엇을 되새겼는지의 관통선**을 적고, 자기 검증 절에서는
"이게 정말 내가 배운 것인가, 그럴듯해서 받아적은 것인가"를 묻는다.
써먹어 본 것과 아직 읽은 것에 그친 것을 갈라서 적을 것.

되새김 글은 H2 5~6개, 500~700단어까지 가도 된다. 위의 3~4개·150~350단어는
판단 글 기준이다.

그리고 **논증을 잠언으로 눌러 담지 마라.** 되새김 글에서 제일 자주 나는 사고가
이것이다. 대화에서 나온 생각에 구조가 있었다면(이렇게 틀리는 경우와 저렇게
틀리는 경우, 왜 그런지의 메커니즘, 뒷받침하는 구체적 사례) 그 구조를 살려서 쓴다.
멋있는 한 줄로 요약하면 읽기엔 매끄러운데 6개월 뒤에 근거를 재구성할 수 없다.
"항공, 자동차, 광케이블"보다 "미국에서 명멸한 2,000개가 넘는 자동차 회사"가 낫다.

### 출력 형식

아래 두 개를 그대로 출력해줘. 설명은 붙이지 말고.

**1) `posts/<id>.md` 에 넣을 본문** — 마크다운 그대로.

**2) `posts/index.json` 맨 위에 넣을 메타 한 줄:**

```json
{
  "id": "YYYY-MM-슬러그",
  "title": "판단이 담긴 제목",
  "date": "YYYY-MM",
  "tag": "5개 중 하나",
  "tickers": ["대문자", "티커"],
  "summary": "핵심 판단 1~2문장."
}
```

메타 규칙:
- `id` — `YYYY-MM-` 뒤에 영문 소문자와 하이픈만. 파일명과 정확히 일치해야 한다.
  예: `2026-04-bigtech-weight`, `2025-08-schd-to-unh`
- `title` — 무슨 일이 있었는지가 드러나야 한다. 티커를 앞에 두면 훑어보기 좋다.
  긴 부연은 `—` 뒤에 붙인다. 예: `BTC · COIN · MSTR — 아직은 관망`
- `tickers` — 본문에 실제로 나온 것만, 중요한 순서대로.
- `summary` — 홈 목록에서 제목 아래 붙는 줄. 제목을 반복하지 말고 **판단의 이유나 기준**을 적는다.
  예: "섹터 비중을 에너지 인프라로 옮기고 싶다는 게 진짜 이유. '느낌'으로 종목을 고르지 말 것."

### (여기까지 복사)

---

## 참고: 기존 3편이 쓴 H2 흐름

| 글 | H2 구성 | 자기 검증 절 |
|---|---|---|
| `2025-08-schd-to-unh` | 왜 UNH였나 · 팔란티어 메모 · 앞으로 | 기회비용을 감수한 근거를 계산으로 확인 |
| `2026-03-btc-coin-mstr` | 세 가지는 다른 베팅이다 · 내 조심스러움의 정체 · 들어간다면 | 합리적 조심 vs 심리적 공포 구분 |
| `2026-04-bigtech-weight` | 진짜 이유부터 분리하기 · 데이터로 확인한 것 · 결론(잠정) | 논리 vs "느낌" 분리, 숫자로 반증 |

세 편 모두 **가운데에 자기 검증 절**이 있다. 이게 이 원장을 단순 매매일지와 구분 짓는 지점이다.
1단계에서 `감정·직관이 개입한 지점`을 따로 뽑는 이유가 이것이다.

## 올리는 법

1. 2단계에서 나온 본문을 `posts/<id>.md` 로 저장
2. 메타 한 줄을 `posts/index.json` **맨 위**에 추가 (쉼표 주의)
3. `git add . && git commit && git push` — 1~2분 뒤 반영

Claude Code에 1단계 결과와 2단계 프롬프트를 주면 1~3을 한 번에 해준다.
